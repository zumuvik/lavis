name: review-fix-helper

on:
  push:
    branches: [feat/module-api-v6-full-telegram]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]' && contains(github.event.head_commit.message, 'chore: apply review fix helper')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/module-api-v6-full-telegram
          fetch-depth: 0
      - name: Apply second-review fixes
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path

          def replace_once(path, old, new):
              p = Path(path)
              text = p.read_text()
              if old not in text:
                  raise SystemExit(f"anchor missing in {path}: {old[:120]!r}")
              p.write_text(text.replace(old, new, 1))

          # Distinguish supervisor backpressure and writer loss at the public error boundary.
          replace_once('src/error.rs',
          '''    #[error("module is unavailable or crashed")]
              Unavailable,
              #[error("invalid argument")]
          ''',
          '''    #[error("module is unavailable or crashed")]
              Unavailable,
              #[error("module IPC queue is full")]
              Backpressure,
              #[error("module IPC writer is unavailable")]
              WriterUnavailable,
              #[error("invalid argument")]
          ''')

          # Trusted core-generated lifecycle frames are bounded by the transport line, not
          # by the hostile module-input per-string guard.
          replace_once('src/external_modules/protocol.rs',
          '''    guard_v6_json(&value, 0).map_err(|_| ExternalError::ProtocolEncode)?;
              let line = serde_json::to_string(&value).map_err(|_| ExternalError::ProtocolEncode)?;
          ''',
          '''    // This value is generated by Lavis, not supplied by an external module.
              // Inbound module JSON keeps the strict depth/string/collection guard; outbound
              // lifecycle frames are constrained by the documented JSON-line transport bound.
              let line = serde_json::to_string(&value).map_err(|_| ExternalError::ProtocolEncode)?;
          ''')

          # Give RPC deadlines their own category rather than pretending they are transport loss.
          replace_once('src/external_modules/v6_executor.rs',
          '''    Transport,
              InvalidResponse,
          ''',
          '''    Transport,
              Timeout,
              InvalidResponse,
          ''')

          # Enrich retained crash records without breaking the legacy builder call sites.
          replace_once('src/external_modules/process.rs',
          '''    time::Duration,
          ''',
          '''    time::{Duration, SystemTime, UNIX_EPOCH},
          ''')
          p = Path('src/external_modules/process.rs')
          text = p.read_text()
          start = text.index('#[derive(Debug, Clone, PartialEq, Eq)]\npub(crate) struct CrashDiagnostics')
          end = text.index('\npub(crate) fn log_module_message', start)
          replacement = r'''#[derive(Debug, Clone, PartialEq, Eq)]
          pub(crate) struct CrashDiagnostics {
              pub(crate) module_id: String,
              pub(crate) protocol_version: u32,
              pub(crate) lifecycle_stage: String,
              pub(crate) request_id: String,
              pub(crate) error_category: String,
              pub(crate) error: String,
              pub(crate) exit_code: Option<i32>,
              pub(crate) signal: Option<i32>,
              pub(crate) stderr_truncated: bool,
              pub(crate) stderr: String,
              pub(crate) timestamp_unix_ms: u64,
              pub(crate) restart_generation: u64,
          }

          impl CrashDiagnostics {
              pub(crate) fn render_user(&self) -> String {
                  let exit = match (self.exit_code, self.signal) {
                      (Some(code), _) => format!("exit={code}"),
                      (_, Some(signal)) => format!("signal={signal}"),
                      _ => "exit=unknown".to_owned(),
                  };
                  let stderr = if self.stderr.is_empty() {
                      "-".to_owned()
                  } else {
                      self.stderr.clone()
                  };
                  format!(
                      "stage={}\ncategory={}\nrequest={}\n{}\ntimestamp_ms={}\ngeneration={}\nstderr_truncated={}\nstderr={}",
                      self.lifecycle_stage,
                      self.error_category,
                      self.request_id,
                      exit,
                      self.timestamp_unix_ms,
                      self.restart_generation,
                      self.stderr_truncated,
                      stderr,
                  )
              }
          }

          /// Backwards-compatible legacy diagnostic builder. V6 uses the richer context-aware
          /// builder below so supervisor stage and process-exit details are retained.
          pub(crate) fn build_crash_diagnostics(
              descriptor: &ExternalModuleDescriptor,
              request_id: Option<&str>,
              error: &ExternalError,
              capture: &StderrCapture,
          ) -> CrashDiagnostics {
              build_crash_diagnostics_with_context(
                  descriptor,
                  request_id,
                  error,
                  capture,
                  "runtime",
                  "runtime_failure",
                  None,
                  None,
                  0,
              )
          }

          pub(crate) fn build_crash_diagnostics_with_context(
              descriptor: &ExternalModuleDescriptor,
              request_id: Option<&str>,
              error: &ExternalError,
              capture: &StderrCapture,
              lifecycle_stage: &str,
              error_category: &str,
              exit_code: Option<i32>,
              signal: Option<i32>,
              restart_generation: u64,
          ) -> CrashDiagnostics {
              let timestamp_unix_ms = SystemTime::now()
                  .duration_since(UNIX_EPOCH)
                  .unwrap_or_default()
                  .as_millis()
                  .min(u128::from(u64::MAX)) as u64;
              CrashDiagnostics {
                  module_id: descriptor.id.clone(),
                  protocol_version: descriptor.protocol_version,
                  lifecycle_stage: lifecycle_stage.to_owned(),
                  request_id: request_id.unwrap_or("-").to_owned(),
                  error_category: error_category.to_owned(),
                  error: error.to_string(),
                  exit_code,
                  signal,
                  stderr_truncated: capture.truncated,
                  stderr: capture.lossy_text().trim().to_owned(),
                  timestamp_unix_ms,
                  restart_generation,
              }
          }

          pub(crate) fn emit_crash_event(diagnostics: &CrashDiagnostics) {
              tracing::error!(
                  event = "external_module_crashed",
                  module_id = %diagnostics.module_id,
                  protocol_version = diagnostics.protocol_version,
                  lifecycle_stage = %diagnostics.lifecycle_stage,
                  request_id = %diagnostics.request_id,
                  error_category = %diagnostics.error_category,
                  error = %diagnostics.error,
                  exit_code = ?diagnostics.exit_code,
                  signal = ?diagnostics.signal,
                  timestamp_unix_ms = diagnostics.timestamp_unix_ms,
                  restart_generation = diagnostics.restart_generation,
                  stderr_truncated = diagnostics.stderr_truncated,
                  stderr = %diagnostics.stderr,
                  "External module crashed"
              );
          }
          '''
          p.write_text(text[:start] + replacement + text[end:])

          # V6 process: retained state, real force terminate, classified queue failures,
          # clean/non-zero shutdown distinction, retained rich diagnostics.
          p = Path('src/external_modules/v6_process.rs')
          text = p.read_text()
          text = text.replace(
          '''pub(crate) struct V6Process {
              control: mpsc::Sender<Control>,
              descriptor: Arc<ExternalModuleDescriptor>,
          }
          ''',
          '''pub(crate) struct V6Process {
              control: mpsc::Sender<Control>,
              descriptor: Arc<ExternalModuleDescriptor>,
              runtime: Arc<Mutex<V6RuntimeState>>,
          }

          #[derive(Debug, Clone)]
          struct V6RuntimeState {
              status: process::ProcessStatus,
              diagnostic: Option<process::CrashDiagnostics>,
          }

          fn lock_runtime_state(state: &Mutex<V6RuntimeState>) -> std::sync::MutexGuard<'_, V6RuntimeState> {
              match state.lock() {
                  Ok(guard) => guard,
                  Err(poisoned) => poisoned.into_inner(),
              }
          }
          ''', 1)
          text = text.replace(
          '''        let stderr_capture = Arc::new(Mutex::new(StderrCapture::default()));
                  let stderr_drain = tokio::spawn(process::drain_stderr(stderr, stderr_capture.clone()));
                  tokio::spawn(supervise(
                      child,
                      process_group,
                      descriptor.clone(),
                      executor,
                      SupervisorIo {
          ''',
          '''        let stderr_capture = Arc::new(Mutex::new(StderrCapture::default()));
                  let stderr_drain = tokio::spawn(process::drain_stderr(stderr, stderr_capture.clone()));
                  let runtime = Arc::new(Mutex::new(V6RuntimeState {
                      status: process::ProcessStatus::Running,
                      diagnostic: None,
                  }));
                  tokio::spawn(supervise(
                      child,
                      process_group,
                      descriptor.clone(),
                      executor,
                      runtime.clone(),
                      SupervisorIo {
          ''', 1)
          text = text.replace(
          '''        Ok(Self {
                      control: control_tx,
                      descriptor: Arc::new(descriptor),
                  })
          ''',
          '''        Ok(Self {
                      control: control_tx,
                      descriptor: Arc::new(descriptor),
                      runtime,
                  })
          ''', 1)
          old_status = '''    pub(crate) fn status(&self) -> super::process::ProcessStatus {
                  if self.control.is_closed() {
                      super::process::ProcessStatus::Crashed
                  } else {
                      super::process::ProcessStatus::Running
                  }
              }
          '''
          new_status = '''    pub(crate) fn status(&self) -> super::process::ProcessStatus {
                  lock_runtime_state(&self.runtime).status
              }

              pub(crate) fn diagnostic(&self) -> Option<process::CrashDiagnostics> {
                  lock_runtime_state(&self.runtime).diagnostic.clone()
              }
          '''
          if old_status not in text:
              raise SystemExit('v6 status anchor missing')
          text = text.replace(old_status, new_status, 1)
          text = text.replace(
          '''    pub(crate) async fn terminate(&self) {
                  let _ = self.graceful_shutdown().await;
              }
          ''',
          '''    pub(crate) async fn terminate(&self) {
                  let (reply, response) = oneshot::channel();
                  if self.control.send(Control::ForceTerminate { reply }).await.is_ok() {
                      let _ = response.await;
                  }
              }
          ''', 1)
          text = text.replace(
          '''    Shutdown {
                  request_id: String,
                  reply: oneshot::Sender<Result<(), ExternalError>>,
              },
          }
          ''',
          '''    Shutdown {
                  request_id: String,
                  reply: oneshot::Sender<Result<(), ExternalError>>,
              },
              ForceTerminate {
                  reply: oneshot::Sender<()>,
              },
          }
          ''', 1)
          text = text.replace(
          '''    ExecutionTimeout,
              ShutdownTimeout,
          }
          ''',
          '''    ExecutionTimeout,
              ShutdownTimeout,
              Backpressure,
              WriterUnavailable,
          }
          ''', 1)
          text = text.replace(
          '''            Self::ExecutionTimeout => ExternalError::ExecutionTimeout,
                      Self::ShutdownTimeout => ExternalError::ShutdownTimeout,
                  }
              }

              fn from_inbound(error: &ExternalError) -> Self {
          ''',
          '''            Self::ExecutionTimeout => ExternalError::ExecutionTimeout,
                      Self::ShutdownTimeout => ExternalError::ShutdownTimeout,
                      Self::Backpressure => ExternalError::Backpressure,
                      Self::WriterUnavailable => ExternalError::WriterUnavailable,
                  }
              }

              fn category(self) -> &'static str {
                  match self {
                      Self::Unavailable => "unavailable",
                      Self::ProtocolEncode => "protocol_encode",
                      Self::ProtocolDecode => "protocol_decode",
                      Self::LineTooLarge => "line_too_large",
                      Self::WrongRequestId => "wrong_request_id",
                      Self::WrongModuleId => "wrong_module_id",
                      Self::ExecutionTimeout => "execution_timeout",
                      Self::ShutdownTimeout => "shutdown_timeout",
                      Self::Backpressure => "backpressure",
                      Self::WriterUnavailable => "writer_unavailable",
                  }
              }

              fn from_writer(error: WriterQueueFailure) -> Self {
                  match error {
                      WriterQueueFailure::Backpressure => Self::Backpressure,
                      WriterQueueFailure::Closed => Self::WriterUnavailable,
                  }
              }

              fn from_inbound(error: &ExternalError) -> Self {
          ''', 1)

          start = text.index('async fn supervise(')
          end = text.index('\nasync fn read_stdout(', start)
          supervisor = r'''async fn supervise(
              mut child: Child,
              process_group: u32,
              descriptor: ExternalModuleDescriptor,
              executor: Arc<dyn V6TelegramExecutor>,
              runtime: Arc<Mutex<V6RuntimeState>>,
              io: SupervisorIo,
          ) {
              let SupervisorIo {
                  mut control_rx,
                  mut reader_rx,
                  writer_tx,
                  mut rpc_rx,
                  actor_tx,
                  reader,
                  writer,
                  stderr_drain,
                  stderr_capture,
              } = io;
              let mut pending = HashMap::new();
              let mut active_calls = HashSet::new();
              let mut workers = JoinSet::new();
              let mut closing = false;
              let mut shutdown_reply: Option<oneshot::Sender<Result<(), ExternalError>>> = None;
              let mut force_reply: Option<oneshot::Sender<()>> = None;
              let mut shutdown_deadline = None;
              let mut shutdown_flushed = false;
              let mut stored_child_exit = None;
              let mut child_reaped = false;
              let mut reader_open = true;
              let mut control_open = true;
              let mut fatal_reason = None;
              let mut fatal_request_id = None;
              let mut fatal_stage = "runtime";
              let mut terminal_status = process::ProcessStatus::Crashed;
              let mut exit_code = None;
              let mut exit_signal = None;

              loop {
                  tokio::select! {
                      child_exit = child.wait(), if stored_child_exit.is_none() => {
                          capture_exit_status(&child_exit, &mut exit_code, &mut exit_signal);
                          child_reaped = child_exit.is_ok();
                          if closing {
                              stored_child_exit = Some(child_exit);
                              if shutdown_flushed {
                                  let result = shutdown_child_exit_result(true, stored_child_exit.take().expect("stored exit"));
                                  let clean = result.is_ok();
                                  if let Some(reply) = shutdown_reply.take() { let _ = reply.send(result); }
                                  if clean {
                                      terminal_status = process::ProcessStatus::Terminated;
                                  } else {
                                      fatal_reason = Some(FatalReason::Unavailable);
                                      fatal_stage = "shutdown";
                                  }
                                  break;
                              }
                          } else {
                              fatal_reason = Some(FatalReason::Unavailable);
                              fatal_stage = pending_stage(&pending);
                              break;
                          }
                      }
                      control = control_rx.recv(), if control_open => match control {
                          Some(Control::Request { frame, expected, reply }) => {
                              if closing { let _ = reply.send(Err(ExternalError::Unavailable)); continue; }
                              let request_stage = outbound_stage(&frame);
                              let Some(request_id) = request_id(&frame).map(str::to_owned) else { let _ = reply.send(Err(ExternalError::ProtocolEncode)); continue; };
                              if pending.contains_key(&request_id) { let _ = reply.send(Err(ExternalError::WrongRequestId)); continue; }
                              if pending.len() == V6_MAX_PENDING { let _ = reply.send(Err(ExternalError::Backpressure)); continue; }
                              if let Err(error) = queue_writer(&writer_tx, WriterCommand::Frame(frame, Flush::None)) {
                                  let reason = FatalReason::from_writer(error);
                                  let _ = reply.send(Err(reason.error()));
                                  fatal_reason = Some(reason);
                                  fatal_request_id = Some(request_id);
                                  fatal_stage = request_stage;
                                  break;
                              }
                              pending.insert(request_id, Pending { expected, deadline: Instant::now() + V6_LIFECYCLE_TIMEOUT, reply });
                          }
                          Some(Control::Shutdown { request_id, reply }) => {
                              if closing { let _ = reply.send(Err(ExternalError::Unavailable)); }
                              else if let Err(reason) = start_shutdown(&mut closing, &mut pending, &mut workers, &writer_tx, request_id, Some(reply), &mut shutdown_reply) {
                                  fatal_reason = Some(reason);
                                  fatal_stage = "shutdown";
                                  break;
                              }
                          }
                          Some(Control::ForceTerminate { reply }) => {
                              closing = true;
                              fail_pending(&mut pending);
                              workers.abort_all();
                              force_reply = Some(reply);
                              terminal_status = process::ProcessStatus::Terminated;
                              break;
                          }
                          None => {
                              control_open = false;
                              if !closing
                                  && let Err(reason) = start_shutdown(&mut closing, &mut pending, &mut workers, &writer_tx, "0".to_owned(), None, &mut shutdown_reply)
                              {
                                  fatal_reason = Some(reason);
                                  fatal_stage = "shutdown";
                                  break;
                              }
                          }
                      },
                      event = reader_rx.recv(), if reader_open => match event {
                          Some(ActorEvent::Inbound(Ok(frame))) => match frame {
                              V6InboundFrame::TelegramInvoke(V6ModuleFrame::TelegramInvoke { call_id, method, params }) => {
                                  if !reserve_call_id(&mut active_calls, &call_id) {
                                      fatal_reason = Some(FatalReason::ProtocolDecode);
                                      fatal_stage = "rpc";
                                      break;
                                  }
                                  if closing {
                                      let rejected = V6CallError { kind: "shutdown".to_owned(), message: "module is shutting down".to_owned() };
                                      if let Err(error) = queue_writer(&writer_tx, WriterCommand::Frame(V6OutboundCoreFrame::TelegramResult { call_id: call_id.clone(), result: Err(rejected) }, Flush::Call(call_id))) {
                                          fatal_reason = Some(FatalReason::from_writer(error));
                                          fatal_stage = "rpc";
                                          break;
                                      }
                                      continue;
                                  }
                                  if active_calls.len() > V6_MAX_ACTIVE_RPCS {
                                      let error = V6CallError { kind: "capacity".to_owned(), message: "too many active calls".to_owned() };
                                      if let Err(queue_error) = queue_writer(&writer_tx, WriterCommand::Frame(V6OutboundCoreFrame::TelegramResult { call_id: call_id.clone(), result: Err(error) }, Flush::Call(call_id))) {
                                          fatal_reason = Some(FatalReason::from_writer(queue_error));
                                          fatal_stage = "rpc";
                                          break;
                                      }
                                      continue;
                                  }
                                  match validate_invoke(&descriptor, &method, true) {
                                      Ok(method) => {
                                          let executor = executor.clone();
                                          let module_id: Arc<str> = Arc::from(descriptor.id.as_str());
                                          let tx = actor_tx.clone();
                                          workers.spawn(async move {
                                              let result = match timeout(V6_RPC_TIMEOUT, executor.execute(V6ExecutionContext { module_id }, method, params)).await {
                                                  Ok(result) => result,
                                                  Err(_) => Err(V6ExecutorError::Timeout),
                                              };
                                              let _ = tx.send(ActorEvent::RpcComplete { call_id, result: map_executor_result(result) }).await;
                                          });
                                      }
                                      Err(error) => {
                                          if let Err(queue_error) = queue_writer(&writer_tx, WriterCommand::Frame(V6OutboundCoreFrame::TelegramResult { call_id: call_id.clone(), result: Err(error) }, Flush::Call(call_id))) {
                                              fatal_reason = Some(FatalReason::from_writer(queue_error));
                                              fatal_stage = "rpc";
                                              break;
                                          }
                                      }
                                  }
                              }
                              frame if terminal_request_id(&frame).is_some() => {
                                  let Some(request_id) = terminal_request_id(&frame).map(str::to_owned) else {
                                      fatal_reason = Some(FatalReason::ProtocolDecode);
                                      break;
                                  };
                                  if let Some(pending_request) = pending.remove(&request_id) {
                                      let stage = expected_stage(pending_request.expected);
                                      if !initialized_module_matches(&descriptor, &frame) {
                                          let _ = pending_request.reply.send(Err(ExternalError::WrongModuleId));
                                          fatal_reason = Some(FatalReason::WrongModuleId);
                                          fatal_request_id = Some(request_id);
                                          fatal_stage = stage;
                                          break;
                                      } else if expected_matches(pending_request.expected, &frame) {
                                          let _ = pending_request.reply.send(Ok(frame));
                                      } else {
                                          let _ = pending_request.reply.send(Err(ExternalError::ProtocolDecode));
                                          fatal_reason = Some(FatalReason::ProtocolDecode);
                                          fatal_request_id = Some(request_id);
                                          fatal_stage = stage;
                                          break;
                                      }
                                  } else {
                                      fatal_reason = Some(FatalReason::WrongRequestId);
                                      fatal_request_id = Some(request_id);
                                      fatal_stage = "runtime";
                                      break;
                                  }
                              }
                              V6InboundFrame::Log { request_id, level, message } => {
                                  if !pending.contains_key(&request_id) {
                                      fatal_reason = Some(FatalReason::WrongRequestId);
                                      fatal_request_id = Some(request_id);
                                      fatal_stage = "runtime";
                                      break;
                                  }
                                  process::log_module_message(&descriptor.id, &request_id, &level, &message);
                              }
                              _ => {
                                  fatal_reason = Some(FatalReason::ProtocolDecode);
                                  fatal_stage = pending_stage(&pending);
                                  break;
                              },
                          },
                          Some(ActorEvent::Inbound(Err(error))) => {
                              fatal_reason = Some(FatalReason::from_inbound(&error));
                              fatal_request_id = single_pending_request_id(&pending);
                              fatal_stage = pending_stage(&pending);
                              break;
                          },
                          Some(ActorEvent::ReaderEof) | None => {
                              reader_open = false;
                              if reader_eof_is_fatal(closing) {
                                  if let Some(reply) = shutdown_reply.take() { let _ = reply.send(Err(ExternalError::Unavailable)); }
                                  fatal_reason = Some(FatalReason::Unavailable);
                                  fatal_request_id = single_pending_request_id(&pending);
                                  fatal_stage = pending_stage(&pending);
                                  break;
                              }
                          },
                          _ => {}
                      },
                      event = rpc_rx.recv() => match event {
                          Some(ActorEvent::RpcComplete { call_id, result }) => {
                              if let Err(error) = queue_writer(&writer_tx, WriterCommand::Frame(V6OutboundCoreFrame::TelegramResult { call_id: call_id.clone(), result }, Flush::Call(call_id))) {
                                  fatal_reason = Some(FatalReason::from_writer(error));
                                  fatal_stage = "rpc";
                                  break;
                              }
                          }
                          Some(ActorEvent::Flushed(call_id)) => { active_calls.remove(&call_id); }
                          Some(ActorEvent::ShutdownFlushed) => {
                              shutdown_flushed = true;
                              shutdown_deadline = Some(Instant::now() + V6_SHUTDOWN_TIMEOUT);
                              if let Some(child_exit) = stored_child_exit.take() {
                                  let result = shutdown_child_exit_result(true, child_exit);
                                  let clean = result.is_ok();
                                  if let Some(reply) = shutdown_reply.take() { let _ = reply.send(result); }
                                  if clean {
                                      terminal_status = process::ProcessStatus::Terminated;
                                  } else {
                                      fatal_reason = Some(FatalReason::Unavailable);
                                      fatal_stage = "shutdown";
                                  }
                                  break;
                              }
                          }
                          Some(ActorEvent::WriterFailed) | None => {
                              if let Some(reply) = shutdown_reply.take() { let _ = reply.send(Err(ExternalError::WriterUnavailable)); }
                              fatal_reason = Some(FatalReason::WriterUnavailable);
                              fatal_request_id = single_pending_request_id(&pending);
                              fatal_stage = if closing { "shutdown" } else { pending_stage(&pending) };
                              break;
                          },
                          _ => {}
                      },
                      _ = async { if let Some(deadline) = shutdown_deadline { tokio::time::sleep_until(deadline).await } else { future::pending::<()>().await } }, if closing => {
                          if let Some(reply) = shutdown_reply.take() { let _ = reply.send(Err(ExternalError::ShutdownTimeout)); }
                          fatal_reason = Some(FatalReason::ShutdownTimeout);
                          fatal_stage = "shutdown";
                          break;
                      },
                      _ = sleep_until_pending_deadline(&pending), if !pending.is_empty() => {
                          if let Some(request_id) = expired_request_id(&pending) {
                              fatal_stage = pending.get(&request_id).map(|request| expected_stage(request.expected)).unwrap_or("runtime");
                              fatal_reason = Some(FatalReason::ExecutionTimeout);
                              fatal_request_id = Some(request_id);
                              break;
                          }
                      },
                  }
                  while workers.try_join_next().is_some() {}
              }

              if fatal_request_id.is_none() {
                  fatal_request_id = single_pending_request_id(&pending);
              }
              if let Some(reason) = fatal_reason {
                  lock_runtime_state(&runtime).status = process::ProcessStatus::Crashed;
                  fail_pending_with(&mut pending, reason);
              } else {
                  fail_pending(&mut pending);
              }
              workers.abort_all();
              while workers.join_next().await.is_some() {}
              drop(writer_tx);
              reader.abort();
              writer.abort();

              kill_group(process_group);
              if !child_reaped {
                  match timeout(V6_SHUTDOWN_TIMEOUT, child.wait()).await {
                      Ok(result) => {
                          capture_exit_status(&result, &mut exit_code, &mut exit_signal);
                      }
                      Err(_) => {
                          let _ = child.kill().await;
                          let result = child.wait().await;
                          capture_exit_status(&result, &mut exit_code, &mut exit_signal);
                      }
                  }
              }

              process::finish_stderr_drain(stderr_drain).await;
              let _ = reader.await;
              let _ = writer.await;

              if let Some(reason) = fatal_reason {
                  let capture = process::lock_capture(&stderr_capture).clone();
                  let error = reason.error();
                  let diagnostics = process::build_crash_diagnostics_with_context(
                      &descriptor,
                      fatal_request_id.as_deref(),
                      &error,
                      &capture,
                      fatal_stage,
                      reason.category(),
                      exit_code,
                      exit_signal,
                      0,
                  );
                  {
                      let mut state = lock_runtime_state(&runtime);
                      state.status = process::ProcessStatus::Crashed;
                      state.diagnostic = Some(diagnostics.clone());
                  }
                  process::emit_crash_event(&diagnostics);
              } else {
                  lock_runtime_state(&runtime).status = terminal_status;
              }
              if let Some(reply) = force_reply {
                  let _ = reply.send(());
              }
          }

          #[derive(Clone, Copy)]
          enum WriterQueueFailure {
              Backpressure,
              Closed,
          }

          fn queue_writer(
              writer: &mpsc::Sender<WriterCommand>,
              command: WriterCommand,
          ) -> Result<(), WriterQueueFailure> {
              match writer.try_send(command) {
                  Ok(()) => Ok(()),
                  Err(mpsc::error::TrySendError::Full(_)) => Err(WriterQueueFailure::Backpressure),
                  Err(mpsc::error::TrySendError::Closed(_)) => Err(WriterQueueFailure::Closed),
              }
          }

          fn reserve_call_id(active_calls: &mut HashSet<String>, call_id: &str) -> bool {
              active_calls.insert(call_id.to_owned())
          }

          fn start_shutdown(
              closing: &mut bool,
              pending: &mut HashMap<String, Pending>,
              workers: &mut JoinSet<()>,
              writer: &mpsc::Sender<WriterCommand>,
              request_id: String,
              reply: Option<oneshot::Sender<Result<(), ExternalError>>>,
              shutdown_reply: &mut Option<oneshot::Sender<Result<(), ExternalError>>>,
          ) -> Result<(), FatalReason> {
              if *closing {
                  return Ok(());
              }
              *closing = true;
              fail_pending(pending);
              workers.abort_all();
              if let Err(error) = queue_writer(
                  writer,
                  WriterCommand::Frame(
                      V6OutboundCoreFrame::Shutdown { request_id },
                      Flush::Shutdown,
                  ),
              ) {
                  let reason = FatalReason::from_writer(error);
                  if let Some(reply) = reply {
                      let _ = reply.send(Err(reason.error()));
                  }
                  return Err(reason);
              }
              *shutdown_reply = reply;
              Ok(())
          }

          fn expected_stage(expected: Expected) -> &'static str {
              match expected {
                  Expected::Initialized => "initialize",
                  Expected::Health => "health",
                  Expected::Result => "execute",
                  Expected::EventResult => "event",
              }
          }

          fn outbound_stage(frame: &V6OutboundCoreFrame) -> &'static str {
              match frame {
                  V6OutboundCoreFrame::Initialize { .. } => "initialize",
                  V6OutboundCoreFrame::Execute { .. } => "execute",
                  V6OutboundCoreFrame::Event { .. } => "event",
                  V6OutboundCoreFrame::Health { .. } => "health",
                  V6OutboundCoreFrame::Shutdown { .. } => "shutdown",
                  V6OutboundCoreFrame::TelegramResult { .. } => "rpc",
              }
          }

          fn pending_stage(pending: &HashMap<String, Pending>) -> &'static str {
              if pending.len() == 1 {
                  pending.values().next().map(|request| expected_stage(request.expected)).unwrap_or("runtime")
              } else {
                  "runtime"
              }
          }

          fn capture_exit_status(
              result: &std::io::Result<std::process::ExitStatus>,
              exit_code: &mut Option<i32>,
              signal: &mut Option<i32>,
          ) {
              if let Ok(status) = result {
                  *exit_code = status.code();
                  #[cfg(unix)]
                  {
                      use std::os::unix::process::ExitStatusExt;
                      *signal = status.signal();
                  }
              }
          }
          '''
          p.write_text(text[:start] + supervisor + text[end:])

          # Shutdown success means exit 0, not merely that wait() returned an ExitStatus.
          replace_once('src/external_modules/v6_process.rs',
          '''    child_exit
                  .map(|_| ())
                  .map_err(|_| ExternalError::Unavailable)
          ''',
          '''    match child_exit {
                  Ok(status) if status.success() => Ok(()),
                  Ok(_) | Err(_) => Err(ExternalError::Unavailable),
              }
          ''')
          replace_once('src/external_modules/v6_process.rs',
          '''        V6ExecutorError::Transport => V6CallError {
                      kind: "transport".to_owned(),
                      message: "Telegram transport unavailable".to_owned(),
                  },
                  V6ExecutorError::InvalidResponse => V6CallError {
          ''',
          '''        V6ExecutorError::Transport => V6CallError {
                      kind: "transport".to_owned(),
                      message: "Telegram transport unavailable".to_owned(),
                  },
                  V6ExecutorError::Timeout => V6CallError {
                      kind: "timeout".to_owned(),
                      message: "Telegram RPC deadline exceeded".to_owned(),
                  },
                  V6ExecutorError::InvalidResponse => V6CallError {
          ''')

          # Retain V6 diagnostics through the manager and expose a sanitized user rendering.
          replace_once('src/external_modules/manager.rs',
          '''    fn descriptor(&self) -> Option<ExternalModuleDescriptor> {
                  match self {
                      Self::Legacy(process) => process
                          .try_lock()
                          .ok()
                          .map(|process| process.descriptor().clone()),
                      Self::V6(process) => Some(process.descriptor().clone()),
                  }
              }
          }
          ''',
          '''    fn descriptor(&self) -> Option<ExternalModuleDescriptor> {
                  match self {
                      Self::Legacy(process) => process
                          .try_lock()
                          .ok()
                          .map(|process| process.descriptor().clone()),
                      Self::V6(process) => Some(process.descriptor().clone()),
                  }
              }

              fn diagnostic_text(&self) -> Option<String> {
                  match self {
                      Self::Legacy(_) => None,
                      Self::V6(process) => process.diagnostic().map(|diagnostic| diagnostic.render_user()),
                  }
              }
          }
          ''')
          replace_once('src/external_modules/manager.rs',
          '''    pub async fn snapshot(&self) -> ExternalRuntimeSnapshot {
                  let mgr = self.inner.lock().await;
                  ExternalRuntimeSnapshot::from_manager(&mgr)
              }
          ''',
          '''    pub async fn snapshot(&self) -> ExternalRuntimeSnapshot {
                  let mgr = self.inner.lock().await;
                  ExternalRuntimeSnapshot::from_manager(&mgr)
              }

              pub async fn diagnostic_text(&self, module_id: &str) -> Option<String> {
                  let mgr = self.inner.lock().await;
                  mgr.processes.get(module_id).and_then(ManagedProcess::diagnostic_text)
              }
          ''')

          # User-facing lm logs + lm info diagnostics.
          replace_once('src/commands.rs',
          '''        usage: "lm [list|info <id>|install|confirm <approval-id>|cancel <approval-id>|enable <id>|disable <id>]",
          ''',
          '''        usage: "lm [list|info <id>|logs <id>|install|confirm <approval-id>|cancel <approval-id>|enable <id>|disable <id>]",
          ''')
          replace_once('src/commands.rs',
          '''            "lm info <id>",
                      "lm install",
          ''',
          '''            "lm info <id>",
                      "lm logs <id>",
                      "lm install",
          ''')
          replace_once('src/commands.rs',
          '''    Info { id: String },
              Enable { id: String },
          ''',
          '''    Info { id: String },
              Logs { id: String },
              Enable { id: String },
          ''')
          replace_once('src/commands.rs',
          '''        "info" => parse_lm_id(tokens)
                      .map(|id| LmRequest::Info { id })
                      .unwrap_or(LmRequest::Invalid),
                  "enable" => parse_lm_id(tokens)
          ''',
          '''        "info" => parse_lm_id(tokens)
                      .map(|id| LmRequest::Info { id })
                      .unwrap_or(LmRequest::Invalid),
                  "logs" => parse_lm_id(tokens)
                      .map(|id| LmRequest::Logs { id })
                      .unwrap_or(LmRequest::Invalid),
                  "enable" => parse_lm_id(tokens)
          ''')
          replace_once('src/runtime.rs',
          '''            LmRequest::Info { id } => self.lm_info(id).await,
                      LmRequest::Invalid => Response::plain(lm_usage(self.prefix())),
          ''',
          '''            LmRequest::Info { id } => self.lm_info(id).await,
                      LmRequest::Logs { id } => self.lm_logs(id).await,
                      LmRequest::Invalid => Response::plain(lm_usage(self.prefix())),
          ''')
          replace_once('src/runtime.rs',
          '''    async fn lm_info(&self, id: &str) -> Response {
                  let Some(config) = &self.module_control else {
          ''',
          '''    async fn lm_logs(&self, id: &str) -> Response {
                  let Some(handle) = &self.external_manager else {
                      return Response::plain("⚠️ Runtime внешних модулей недоступен.".to_owned());
                  };
                  match handle.diagnostic_text(id).await {
                      Some(diagnostic) => Response::plain(format!("📋 Последняя ошибка модуля {id}\n\n{diagnostic}")),
                      None => Response::plain(format!("ℹ️ Для модуля {id} нет сохранённой runtime-ошибки.")),
                  }
              }

              async fn lm_info(&self, id: &str) -> Response {
                  let Some(config) = &self.module_control else {
          ''')
          # Add runtime diagnostic to info while keeping installation metadata authoritative.
          old = '''        match control::module_info(&config.root, &config.declarative_state_path, &state, id) {
                      Ok(module) => Response::plain(format!(
                          "📦 {}\\nID: {}\\nВерсия: {}\\nАвтор: {}\\nСостояние: {}\\nИсточник: {}\\nТочка входа: {}\\nSchema/API protocol: v{}\\nВозможности: {}\\nПредоставляемые команды: {}",
                          module.display_name,
                          module.id,
                          module.version,
                          module.author,
                          enabled_label(module.enabled),
                          management_label(module.management),
                          module.entrypoint,
                          module.protocol_version,
                          capabilities_label(&module.capabilities),
                          commands_label(&module.commands)
                      )),
          '''
          new = '''        let diagnostic = if let Some(handle) = &self.external_manager {
                      handle.diagnostic_text(id).await
                  } else {
                      None
                  };
                  match control::module_info(&config.root, &config.declarative_state_path, &state, id) {
                      Ok(module) => Response::plain(format!(
                          "📦 {}\\nID: {}\\nВерсия: {}\\nАвтор: {}\\nСостояние: {}\\nИсточник: {}\\nТочка входа: {}\\nSchema/API protocol: v{}\\nВозможности: {}\\nПредоставляемые команды: {}\\nRuntime: {}\\nПоследняя ошибка: {}",
                          module.display_name,
                          module.id,
                          module.version,
                          module.author,
                          enabled_label(module.enabled),
                          management_label(module.management),
                          module.entrypoint,
                          module.protocol_version,
                          capabilities_label(&module.capabilities),
                          commands_label(&module.commands),
                          runtime_status(self, &module.id),
                          diagnostic.as_deref().unwrap_or("нет")
                      )),
          '''
          replace_once('src/runtime.rs', old, new)

          # Update usage helper wherever the literal appears.
          rp = Path('src/runtime.rs')
          rtext = rp.read_text().replace(
              'lm [list|info <id>|install|confirm <approval-id>|cancel <approval-id>|enable <id>|disable <id>]',
              'lm [list|info <id>|logs <id>|install|confirm <approval-id>|cancel <approval-id>|enable <id>|disable <id>]'
          )
          rp.write_text(rtext)

          # Correct the roadmap capability contract and document the split limits.
          replace_once('docs/module-roadmap.md',
          '''Curated helpers:

          - require `telegram.raw` and an explicit entry in `telegram_methods` under the
            current v6 manifest model;
          ''',
          '''Curated helpers:

          - require an explicit entry in `telegram_methods` under the current v6 manifest model;
          - do not require `telegram.raw`; that high-risk capability is reserved for `raw.invoke`;
          ''')

          # Tests for the bugs called out by review.
          pp = Path('src/external_modules/protocol.rs')
          ptext = pp.read_text()
          marker = '#[cfg(test)]\nmod tests {'
          if marker in ptext and 'outbound_lifecycle_allows_strings_above_untrusted_input_guard' not in ptext:
              ptext = ptext.replace(marker, marker + r'''
              #[test]
              fn outbound_lifecycle_allows_strings_above_untrusted_input_guard() {
                  let frame = V6OutboundCoreFrame::Event {
                      request_id: "12".to_owned(),
                      event: MessageEventKind::Created,
                      payload: MessageEvent {
                          event_id: "e".to_owned(),
                          message_ref: "r".to_owned(),
                          message_key: "k".to_owned(),
                          peer_id: None,
                          text: "x".repeat(V6_MAX_JSON_STRING_BYTES + 1),
                          outgoing: false,
                          entities: vec![],
                      },
                  };
                  let line = frame.serialize().expect("trusted outbound event should serialize");
                  assert!(line.len() < MAX_LINE_BYTES);
              }

              #[test]
              fn outbound_lifecycle_still_obeys_line_limit() {
                  let frame = V6OutboundCoreFrame::Event {
                      request_id: "12".to_owned(),
                      event: MessageEventKind::Created,
                      payload: MessageEvent {
                          event_id: "e".to_owned(),
                          message_ref: "r".to_owned(),
                          message_key: "k".to_owned(),
                          peer_id: None,
                          text: "x".repeat(MAX_LINE_BYTES),
                          outgoing: false,
                          entities: vec![],
                      },
                  };
                  assert!(matches!(frame.serialize(), Err(ExternalError::ProtocolEncode)));
              }
          ''', 1)
              pp.write_text(ptext)

          vp = Path('src/external_modules/v6_process.rs')
          vtext = vp.read_text()
          vtext = vtext.replace(
          '''        assert_eq!(
                      v6_executor_error(V6ExecutorError::Rpc {
          ''',
          '''        assert_eq!(
                      v6_executor_error(V6ExecutorError::Timeout).kind,
                      "timeout"
                  );
                  assert_eq!(
                      v6_executor_error(V6ExecutorError::Rpc {
          ''', 1)
          vtext = vtext.replace(
          '''            let exited = Ok(std::process::ExitStatus::from_raw(0));
                      assert!(matches!(
                          shutdown_child_exit_result(false, exited),
                          Err(ExternalError::Unavailable)
                      ));
          ''',
          '''            let exited = Ok(std::process::ExitStatus::from_raw(0));
                      assert!(matches!(
                          shutdown_child_exit_result(false, exited),
                          Err(ExternalError::Unavailable)
                      ));
                      assert!(shutdown_child_exit_result(
                          true,
                          Ok(std::process::ExitStatus::from_raw(0))
                      ).is_ok());
                      assert!(matches!(
                          shutdown_child_exit_result(
                              true,
                              Ok(std::process::ExitStatus::from_raw(1 << 8))
                          ),
                          Err(ExternalError::Unavailable)
                      ));
          ''', 1)
          vp.write_text(vtext)
          PY

          cargo fmt --all -- --check || cargo fmt --all
          cargo check --all-targets
          cargo clippy --all-targets -- -D warnings
          cargo test --all-targets

          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src docs
          git commit -m 'fix: close second Module API v6 review blockers'
          git push origin HEAD:feat/module-api-v6-full-telegram
